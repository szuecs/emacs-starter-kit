# name: junit3 test clcass
# group: test
# key: tc
# --
public class ${1:`(let ((fn (capitalize (file-name-nondirectory
                                 (file-name-sans-extension
				 (or (buffer-file-name)
				     (buffer-name (current-buffer))))))))
           (cond
             ((string-match "_" fn) (replace-match "" nil nil fn))
              (t fn)))`}Test extends ${2:TestCase} {
 	    $0
}

