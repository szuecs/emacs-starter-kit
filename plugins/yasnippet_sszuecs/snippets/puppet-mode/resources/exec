#name : exec {..}
#key: exec
# --
exec {
  "${1:name}":
    command => "${2:command --with args}",
    cwd  => "/var/empty",
    onlyif  => ["${3:another_command}"],
    unless  => ["$3"],
    require => ["${4:Resource['dependency']}"],
}
